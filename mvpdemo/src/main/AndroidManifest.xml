<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.zl.mvpdemo">

    <application android:allowBackup="true" android:icon="@mipmap/my_ic_lanucher"
        android:name=".MyApplication"
        android:label="@string/app_name" android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".view.activity.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.activity.AppBarActivity"
            android:launchMode="singleTask"/>

        <activity android:name=".view.activity.GirlActivity"/>

        <activity android:name=".view.activity.GifActivity"/>

        <activity android:name=".view.activity.SaveActivity"/>

        <activity android:name=".view.activity.GirlPictureActivity" />

        <activity android:name=".view.activity.DayActivity" />

        <activity android:name=".view.activity.WebViewActivity"
            android:launchMode="singleTask"/>


        <meta-data android:name="com.zl.mvpdemo.model.model.MyGlideModule"
            android:value="GlideModule"
            />

    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

</manifest>